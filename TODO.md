# Vite + Vercel Deployment Fix
Current Progress: 4/6 ✅

## Steps:
1. ✅ Create `vercel.json` for static SPA deploy
2. ✅ Update `vite.config.ts` for Vercel static opts
3. ✅ Update `package.json`: Downgrade Vite to 5.4.x, add preview script, remove overrides
4. ✅ Clean install deps
5. 🔄 Test `npm run build` + `npm run preview`
6. 🔄 Push changes & redeploy Vercel
